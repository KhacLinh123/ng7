import { MakeDomBetterDirective } from './make-dom-better.directive';

describe('MakeDomBetterDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeDomBetterDirective();
    expect(directive).toBeTruthy();
  });
});
