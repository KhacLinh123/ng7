import { HandleEventDynamicDirective } from './handle-event-dynamic.directive';

describe('HandleEventDynamicDirective', () => {
  it('should create an instance', () => {
    const directive = new HandleEventDynamicDirective();
    expect(directive).toBeTruthy();
  });
});
